#!/bin/bash

set -u

tag=latest
if [ $# -ge 1 ] ; then
  tag=$1
fi

command=""
if [ $# -ge 2 ] ; then
  command=$2
fi


readonly REPO=kunst1080/x11
readonly CONTAINER=$REPO:$tag

docker run --rm -it --privileged -v /run/udev:/run/udev $CONTAINER $command

